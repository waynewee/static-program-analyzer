procedure Example {
  x = 2;
  z = 3;
  i = 5;
}

procedure p {
  if (x<0) then {
    while (i>0) {
      x = z * 3 + 2 - y;
      i = i - 1; }
    x = x + 1;
    z = x + z; }
  else {
    z = 1; }
  z = z + x + i; }

procedure q {
  if (x==1) then {
    z = x + 1; }
  else {
    x = z + x; } }
