procedure keywordAssignments {
	procedure = x + 1;
	if = if + 1;
	if = if * if;
	while = while % while;
	procedure = procedure + 5;
	then = then * if;
	read = procedure * else;
	print = read - print;
}